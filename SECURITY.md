# Security policy

Report memory corruption, model-validation disagreements, mapping boundary failures, relocation writes outside mapped memory, unbounded allocation or recursion, and lifetime defects privately with a minimized input and sanitizer trace. BinForge does not execute loaded content and must not be treated as an operating-system security boundary.
